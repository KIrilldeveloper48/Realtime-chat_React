const LOGIN_ROUTE = `/login`;
const CHAT_ROUTE = `/chat`;
const MAIN_ROUTE = `/`;

export const Routes = {
  MAIN: MAIN_ROUTE,
  CHAT: CHAT_ROUTE,
  LOGIN: LOGIN_ROUTE
};

export const ENTER_KEYCODE = 13;
